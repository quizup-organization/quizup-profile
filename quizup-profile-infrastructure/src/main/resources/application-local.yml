server:
  port: 8088

spring:
  config:
    import: optional:classpath:quizup-local.yml
    activate:
      on-profile: local

  datasource:
    url: jdbc:postgresql://localhost:5432/quizup_profile
    username: postgres
    password: postgres

app:
  seed-data:
    enabled: true
