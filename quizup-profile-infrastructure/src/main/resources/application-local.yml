server:
  port: 8084

spring:
  config:
    activate:
      on-profile: local

  datasource:
    url: jdbc:postgresql://localhost:5432/quizup_profile
    username: postgres
    password: postgres

app:
  seed-data:
    enabled: true

logging:
  level:
    io.github.quizup: DEBUG

